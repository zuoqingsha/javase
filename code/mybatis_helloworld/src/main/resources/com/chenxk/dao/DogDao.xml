<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.chenxk.dao.DogDao">
    <!--
    在使用mybatis进行查询的时候，mybatis默认会帮我们进行结果的封装，但是要求列名跟属性名称一一对应
    在实际使用过程中，会出现数据库列名与类中的属性名不对应的情况，此时可以采用以下两种方式：
        1、在编写sql语句的时候添加别名
        2、自定义封装结果集
    -->
    <!--根据查询的数据进行结果的封装要使用resultMap属性，表示使用自定义规则-->
    <select id="selectDogById" resultMap="myDog">
        select * from dog where id = #{id}
    </select>

    <!--
    自定义结果集，将每一列的数据与javaBean的对象属性对应起来
        type：表示对哪个javaBean对象进行对应
        id：唯一表示，方便其他属性标签进行引用
    -->
    <resultMap id="myDog" type="Dog">
        <!--指定主键列的对应规则-->
        <id column="id" property="id"></id>
        <!--设置其他列的对应关系-->
        <result column="dname" property="name"></result>
        <result column="dage" property="age"></result>
        <result column="dgender" property="gender"></result>
    </resultMap>

</mapper>