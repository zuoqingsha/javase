<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.chenxk.dao.EmpDao">
    <!--
    select: 表示这个操作是个查询操作
    id: 表示要匹配的方法名称
    resultType: 表示返回值的类型，查询操作必须要包含返回值类型
    #{属性名}: 表示要传递的参数的名称
    -->
    <select id="findEmpByEmpno" resultType="com.chenxk.bean.Emp" databaseId="mysql">
        select * from emp where empno = #{empno}
    </select>

    <select id="findEmpByEmpno" resultType="com.chenxk.bean.Emp" databaseId="orcl">
        select * from emp where empno = #{empno}
    </select>

    <insert id="insertEmp">
        insert into emp(empno,ename) value (#{empno},#{ename})
    </insert>

    <update id="updateEmp">
        update emp set ename = #{ename} where empno = #{empno}
    </update>

    <delete id="deleteEmp">
        delete from emp where empno = #{empno}
    </delete>

    <select id="selectEmpByNoAndName" resultType="Emp">
        select * from emp where empno=#{empno} and ename=#{ename}
    </select>

    <select id="selectEmpByNoAndName2" resultType="Emp">
        select * from emp where empno=#{empno} and ename=#{ename}
    </select>

    <select id="selectEmpByNoAndName3" resultType="Emp">
        select * from ${t} where empno=#{empno} and ename=#{ename}
    </select>

    <select id="selectAllEmp" resultType="Emp">
        select * from emp
    </select>

    <select id="selectEmpByNoReturnMap" resultType="map">
        select * from emp where empno = #{empno}
    </select>

    <select id="selectAllReturnMap" resultType="map">
        select * from emp
    </select>

</mapper>