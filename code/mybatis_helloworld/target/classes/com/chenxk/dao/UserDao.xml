<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.chenxk.dao.UserDao">
    <!--
    select: 表示这个操作是个查询操作
    id: 表示要匹配的方法名称
    resultType: 表示返回值的类型，查询操作必须要包含返回值类型
    #{属性名}: 表示要传递的参数的名称
    -->
    <select id="selectUserById" resultType="com.chenxk.bean.User" databaseId="mysql">
        select * from user where id = #{id}
    </select>

    <insert id="insertUser" useGeneratedKeys="true" keyProperty="id">
        insert into user(user_name) value (#{userName})
    </insert>

    <update id="updateUser">
        update user set user_name = #{userName} where id = #{id}
    </update>

    <delete id="deleteUser">
        delete from user where id = #{id}
    </delete>
</mapper>